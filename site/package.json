{
	"name": "@microsoft/atlas-site",
	"version": "0.0.1",
	"license": "MIT",
	"private": true,
	"scripts": {
		"start": "parcel src/**/*.md --port 1111 --no-cache --log-level verbose",
		"build": "parcel build src/**/*.md",
		"toc": "parcel src/**/*.navigation.json --log-level verbose --no-cache"
	},
	"devDependencies": {
		"@microsoft/atlas-css": "^0.3.0",
		"parcel": "next",
		"@microsoft/parcel-transformer-markdown-html": "^2.0.0-alpha.3.1",
		"fs-extra": "^9.1.0"
	}
}
