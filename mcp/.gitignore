# Generated build artifact: regenerated by `npm run build:data` and shipped
# via package.json "files". Not source — do not commit.
# Leading slash anchors this to mcp/data/ only (not e.g. src/data/).
/data/
